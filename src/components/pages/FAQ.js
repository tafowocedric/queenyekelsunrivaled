import React from 'react';
import PublicContainer from '../container/PublicContainer';

const FAQ = () => {
    return (
        <PublicContainer>
            <div>faq</div>
        </PublicContainer>
    );
};

export default FAQ;
