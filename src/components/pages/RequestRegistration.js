import React from 'react';
import PublicContainer from '../container/PublicContainer';

const RequestRegistration = () => {
    return (
        <PublicContainer>
            <div>request registration</div>
        </PublicContainer>
    );
};

export default RequestRegistration;
